# REST Schema: GoogleOAuthLoginCredentials

type: object
description: The user's login credentials for google-oauth authentication
properties:
  email: { $ref: "../open-api-schema.yaml#/components/schemas/Email" }
  googleIDToken: { $ref: "../open-api-schema.yaml#/components/schemas/GoogleIDToken" }
required:
  - email
  - googleIDToken
