# REST Schema: PasswordResetInitParams

type: object
description: The email address of the user initiating a password reset
properties:
  email: { $ref: "../open-api-schema.yaml#/components/schemas/Email" }
required:
  - email
