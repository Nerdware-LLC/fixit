# REST Schema: LocalLoginCredentials

type: object
description: The user's login credentials for local authentication
properties:
  email: { $ref: "../open-api-schema.yaml#/components/schemas/Email" }
  password: { $ref: "../open-api-schema.yaml#/components/schemas/Password" }
required:
  - email
  - password
