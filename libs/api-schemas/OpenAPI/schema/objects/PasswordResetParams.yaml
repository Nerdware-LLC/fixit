# REST Schema: PasswordResetParams

type: object
description: A new password and a valid password reset token
properties:
  password:
    $ref: "../open-api-schema.yaml#/components/schemas/Password"
  passwordResetToken:
    $ref: "../open-api-schema.yaml#/components/schemas/PasswordResetToken"
required:
  - password
  - passwordResetToken
