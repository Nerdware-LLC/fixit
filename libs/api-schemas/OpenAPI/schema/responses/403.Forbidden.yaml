description: |
  [403 Forbidden] The requesting user is not authorized to perform this action.
content:
  application/json:
    schema:
      $ref: "../open-api-schema.yaml#/components/schemas/Error"
