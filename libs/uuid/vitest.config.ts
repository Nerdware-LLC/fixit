export { commonVitestNodeConfigs as default } from "../../libs/vitest-utils/common-configs/vitest.node-configs.js";

// Note: currently the nx vite plugin breaks if we use path aliases in vite/vitest config files.
